# Elevation enrichment dependencies
# Install with: pip install -r scripts/requirements.txt

# Core geospatial libraries
rasterio>=1.3.0          # Read GeoTIFF elevation data
numpy>=1.24.0            # Numerical operations

# API-based elevation fetching (fallback)
requests>=2.28.0         # HTTP requests for Open Topo Data API

# Optional: for downloading USGS data
# boto3>=1.26.0          # AWS S3 access for some elevation datasets
